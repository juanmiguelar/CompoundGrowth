import { CalculationParams, CalculationResult } from '../types';

export const getFinancialAdvice = async (
  params: CalculationParams,
  result: CalculationResult
): Promise<string> => {
  return "AI integration has been removed from this project.";
};